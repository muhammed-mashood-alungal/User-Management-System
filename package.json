{
  "name": "ums",
  "version": "1.0.0",
  "description": "This is a user management system where users can register and login with a profile photo. Also includes an admin panel where the admin can list and edit all users.",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.5.1"
  }
}
